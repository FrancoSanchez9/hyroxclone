/**
 * Brand design tokens for use in JS/inline styles and Framer Motion.
 * Keep in sync with the CSS `@theme` tokens in `globals.css`.
 * For Tailwind classes prefer the utilities (`bg-rl-accent`, `ease-out-strong`).
 */

/** Brand accent (lime). Mirrors `--color-rl-accent`. */
export const ACCENT = "#d4ff00";

/** Signature easing curve. Mirrors `--ease-out-strong`. */
export { EASE } from "./animation";
