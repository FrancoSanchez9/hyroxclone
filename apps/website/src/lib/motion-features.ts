import { domAnimation } from "framer-motion";

// LazyMotion loads gesture/animation features after the application shell instead
// of placing them in the parser-blocking client entry.
export default domAnimation;
